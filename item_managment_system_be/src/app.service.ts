import { Injectable } from '@nestjs/common';

@Injectable()
export class AppService {
  // This service can be used for application-wide business logic
  // Currently, all business logic is handled by specific module services
}
